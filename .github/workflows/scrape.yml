name: 📅 Daily Scrape

on:
  schedule:
    - cron: '0 6 * * *'       # Todos los días a las 06:00 UTC (08:00 CEST) :contentReference[oaicite:2]{index=2}
  workflow_dispatch:         # Permite disparar manualmente el workflow

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4                          # Clona el repo al runner :contentReference[oaicite:3]{index=3}

      - name: Set up Node.js
        uses: actions/setup-node@v4                        # Configura Node.js y npm :contentReference[oaicite:4]{index=4}
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm ci                                        # Usa package-lock.json para instalar localmente :contentReference[oaicite:5]{index=5}

      - name: Run scraper via npx
        run: npx website-scraper --config sitemap.json --output ./data
                                                             # npx ejecuta el binario localmente instalado :contentReference[oaicite:6]{index=6}

      - name: Upload results
        uses: actions/upload-artifact@v4                   # Sube ./data como artefacto del run :contentReference[oaicite:7]{index=7}
        with:
          name: scrape-results
          path: data/
